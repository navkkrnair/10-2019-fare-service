server:
  port: 8080
spring:
  application:
    name: fare-service
  cloud:
    config:
      uri:
      - http://${CONFIG-SERVICE}:8888
management:
  endpoint:
    health:
      show-details: always      